# PROJECT_CONTEXT

## 项目定位

这是一个面向 ITK China 内部使用的 IT 资产管理小工具项目，最初以 PowerShell 桌面工具实现，后续逐步向公司内网 Web 系统演进。

当前目标不是做通用资产平台，而是优先解决以下日常管理问题：

- 电脑资产登记
- 人员归属维护
- 库存电脑管理
- 归属历史追踪
- 邮件盘点
- NAS 云备份

## 当前开发进度

### 1. 桌面版现状

桌面版 `电脑信息管理工具.ps1` 仍是当前主版本，已经具备：

- 电脑信息新增、编辑、搜索、导出
- 库存电脑管理
- 人员名单管理
- 实习生 / Mentor 规则
- 归属历史查看
- 邮件盘点规则和待发送对象选择
- NAS 云端备份与云端拉取
- 启动时检测云端是否存在更新版本

### 2. 近期已落地的 Web 功能

当前仓库里的 Web 端已经完成了下面这些能力：

- 首页仪表盘统计
- 在用电脑 / 库存电脑 / 人员名单三类列表
- 搜索、详情面板、详情弹窗
- 电脑新增、编辑、转入库存、库存删除
- 人员新增、编辑、删除
- 关键业务规则校验：
  - 序列号必填
  - 固定资产号 / MAC 可为空并展示为 `N/A`
  - 实习生必须绑定 Mentor
  - 被引用为 Mentor 的人员不可删除
  - 删除人员后其电脑自动转库存
  - 编辑电脑需要授权密码 `1`
- 归属历史自动追加
- 邮件盘点迁移到 Web：
  - 候选收件人预览
  - 全选 / 取消全选
  - 跳过原因展示
  - 生成 `mailto:` 草稿
  - 盘点批次记录写入 `inventory_mail_batches.json`
- 列表交互优化：
  - 左右栏高度适配
  - 列表内部滚动
  - 按型号、归属人排序
  - MAC 地址统一格式展示
  - 拼音按 `Pan Ya` 形式展示

### 3. Web 端当前定位

Web 端现在已经具备日常演示和单人轻量维护能力，但还不能视为最终正式系统。

它当前的技术形态是：

- 服务端：Node.js 原生 `http`
- 前端：原生 HTML / CSS / JS
- 数据源：直接读写 `data/*.json`
- 启动方式：`node web-server.js`

## 当前仍未完成的关键点

以下内容仍然是后续正式化时最优先的工作：

- 登录与角色权限
- 数据库替代 JSON，优先推荐 SQLite
- 多人并发写入保护
- 更稳的部署方式和服务化运行
- 更完整的数据编码清理
- 将桌面版剩余功能继续迁移到 Web

## 技术现状

### 桌面版

- 语言：PowerShell
- UI：Windows Forms
- 存储：本地 JSON

### Web 版

- 运行时：Node.js
- 服务端：原生 `http`
- 前端：原生 HTML / CSS / JS
- 存储：本地 JSON
- 默认监听：`0.0.0.0:8099`

## 后续建议路线

建议的正式演进顺序：

1. 保持 Web 端现有功能稳定可用
2. 将存储从 JSON 迁移到 SQLite
3. 增加登录和角色权限
4. 增加记录级并发冲突保护
5. 再考虑桌面版逐步降级为应急工具或只读工具

## 当前最重要的事实

- 桌面版仍是生产主版本
- Web 端已经不是只读预览，而是具备核心 CRUD 和邮件盘点能力
- Web 端当前最大短板不是页面，而是存储、权限和并发控制
- README、PROJECT_CONTEXT、requirements 应始终跟随当前仓库实际状态更新
