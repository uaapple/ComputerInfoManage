# PROJECT_CONTEXT

## 项目定位

这是一个 IT 资产管理小型内部工具项目，最初以 PowerShell 桌面工具实现，后续开始向公司内网 Web 系统演进。

当前目标不是做通用资产平台，而是优先解决 ITK China 电脑资产、归属、库存和盘点的日常管理问题。

## 当前开发进度

### 1. 桌面版现状

桌面版 `电脑信息管理工具.ps1` 是当前主版本，已经具备以下能力：

- 电脑信息新增、编辑、搜索、导出
- 库存电脑管理
- 人员名单管理
- 实习生 / Mentor 规则
- 归属历史查看
- 邮件盘点规则和待发送选择列表
- NAS 云端备份与云端拉取
- 启动时检测云端是否有更新版本

### 2. 近期已完成的需求

以下是近期已经实现过的对话需求摘要：

- 固定资产号支持“暂无”，空值展示为 `N/A`
- MAC 地址支持“暂无”，空值展示为 `N/A`
- 编辑密码从 `123456` 改为 `1`
- 编辑已有记录后取消“保存成功”提示
- 邮件盘点改为先弹出待发送对象勾选列表
- 待发送对象规则调整为：
  - 正式员工本人
  - 实习生对应 Mentor
- 界面文案和部分遮挡布局已做过修正
- 新增 Web 预览版首页
- 新增面向 NAS File Browser 的云端备份机制
- 云端同步加入简单版本保护，避免旧版本覆盖新版本

### 3. Web 化现状

仓库中已新增一个轻量 Web 预览版：

- `web-server.js`
- `web/index.html`
- `web/styles.css`
- `web/app.js`

当前它只是“静态风格 + 读取 JSON 展示”的预览实现，主要用于验证：

- 清新蓝白的视觉方向
- 顶部品牌区与 Logo
- 首页仪表盘 + 列表结构
- 在用电脑 / 库存电脑 / 人员名单三视图
- 搜索与详情弹窗

### 4. Web 版尚未完成的关键点

以下内容还没有真正完成，后续实现时要优先注意：

- 没有正式登录页和用户表
- 没有角色权限控制
- 没有数据库，仍直接读 JSON
- 没有编辑、新增、删除接口
- 没有真正的多人并发保护
- 没有部署成 Windows 服务或开机自启
- 没有解决服务端字符串编码脏数据问题
- 没有将邮件盘点、云端备份迁移为 Web 版能力

## 当前技术形态

### 桌面版

- 语言：PowerShell
- UI：Windows Forms
- 数据存储：本地 JSON
- 运行方式：本地双击 bat 或 PowerShell 执行

### Web 预览版

- 运行时：Node.js
- 服务端：Node 原生 `http`，无外部依赖
- 前端：原生 HTML / CSS / JS
- 数据来源：直接读取 `data/*.json`
- 默认监听：`0.0.0.0:8099`
- 目标部署环境：公司内网 Windows 虚拟机 `10.36.76.95`

## 推荐后续开发方向

建议的正式演进路线：

1. 将 Web 预览版升级为正式 Web 应用
2. 存储从 JSON 迁移到 SQLite
3. 增加登录和角色权限
4. 增加电脑 / 人员 / 库存的增删改查接口
5. 加入记录级并发冲突保护
6. 将桌面版逐步降级为只读或应急工具

## 当前最重要的事实

- 桌面版当前比 Web 版成熟得多
- Web 版只是前端形态预览，不是生产替代版
- 如果继续开发 Web 版，下一步最重要的不是美化，而是数据库、登录、编辑能力和并发控制
- 当前仓库中的 README、PROJECT_CONTEXT、requirements 应作为接力开发的主要入口文档
